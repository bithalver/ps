%!PS-Adobe-3.0
%%DocumentData: Clean7Bit
%%EndComments

% This is the original, it won't work anymore 20030402
quit

my_init

/oa { add dup 12 gt {12 sub} if } def /kv { sub dup  1 lt {12 add} if } def

/leftmargin    2 cm def /rightmargin 1   cm def
/bottommargin  2 cm def /topmargin   1.5 cm def

/threads 18 def

/k 12 15 basicplane def

1 1 4 { /rit exch def
  1 1 4 { /minta exch def
    clearallnets 
    1 1 12 { /so exch def
      so 1 eq
        { k [ so dup 6 oa dup rit oa ] nthpoints pcn }
        { 
          so 1 sub minta mod 0 eq 
          { k [ so dup 6 oa dup rit oa ] nthpoints 4 -1 roll paddn } if 
        } ifelse
    } for
%     /tn exch def tn nadd
    nadd
    gsave fitto1page drawallnets showpage grestore
  } for
} for
